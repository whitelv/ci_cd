name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Workflow trigger confirmation
        run: echo "CI workflow started"
